# Test Environment Variables
# Copy this file and create test users with these credentials in your Supabase project

# Test user credentials (admin role)
TEST_ADMIN_EMAIL=test-admin@pipelinescout.test
TEST_ADMIN_PASSWORD=SecureTestPassword123!

# Test user credentials (member role)
TEST_MEMBER_EMAIL=test-member@pipelinescout.test
TEST_MEMBER_PASSWORD=SecureTestPassword123!

# Test user credentials (viewer role)
TEST_VIEWER_EMAIL=test-viewer@pipelinescout.test
TEST_VIEWER_PASSWORD=SecureTestPassword123!

# Instructions:
# 1. Create these three test users in your Supabase project
# 2. Assign appropriate roles in the user_roles table:
#    - test-admin@pipelinescout.test → admin
#    - test-member@pipelinescout.test → member
#    - test-viewer@pipelinescout.test → viewer
# 3. Run tests with: npm run test:security
